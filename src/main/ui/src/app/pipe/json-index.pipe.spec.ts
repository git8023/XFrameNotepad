import { JsonIndexPipe } from './json-index.pipe';

describe('JsonIndexPipe', () => {
  it('create an instance', () => {
    const pipe = new JsonIndexPipe();
    expect(pipe).toBeTruthy();
  });
});
